export const QUEUES = Object.freeze({
  LINKS: "links",       // link lifecycle events from links-service
  CLICKS: "clicks",     // click events from redirect-service → analytics-service
  ANALYTICS: "analytics",
  NOTIFICATIONS: "notifications",
});
